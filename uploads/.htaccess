# Yüklenen PHP dosyalarının çalıştırılmasını engelle (Apache)
<IfModule mod_php.c>
  php_flag engine off
</IfModule>
<IfModule mod_authz_core.c>
  <FilesMatch "(?i)\.(php|phtml|php5|phar)$">
    Require all denied
  </FilesMatch>
</IfModule>
